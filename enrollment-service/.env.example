# =============================================================================
# Biến môi trường cho enrollment-service (Local & Docker)
# =============================================================================
DB_HOST=localhost
DB_PORT=3306
DB_NAME=enrollment_db
DB_USERNAME=elearning
DB_PASSWORD=elearning

# Khóa JWT (phải khớp với auth-service và shared-common)
JWT_SECRET=elearning-microservices-auth-service-jwt-secret-key-for-local-dev-only-32bytes

# Cổng dịch vụ
SERVER_PORT=8083
